#include "bitmap.h"
#include <iostream>
#include <string>

using namespace std;

//Prototypes
void validImage(Bitmap image);
void getImage(Bitmap input);
void sizeImage(Bitmap size);

int main()
{

    

}

//checks if user input is a valid image
void validImage(Bitmap image){
    image.open(file);
    bool realImage = image.isImage();
    if (realImage == false){
        
    }
    
}

//asks user for image
void getImage(Bitmap input){

}

//checks if new image is the same size as previous image
void sizeImage(Bitmap size){

}
